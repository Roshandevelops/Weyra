import 'package:flutter/material.dart';

class DeliveredTabWidget extends StatelessWidget {
  const DeliveredTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("data"),
    );
  }
}
