import 'package:flutter/material.dart';

class ReturnedTabWidget extends StatelessWidget {
  const ReturnedTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("data"),
    );
  }
}
