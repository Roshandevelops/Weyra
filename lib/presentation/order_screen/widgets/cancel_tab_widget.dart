import 'package:flutter/material.dart';

class CancelTabWidget extends StatelessWidget {
  const CancelTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("data"),
    );
  }
}
