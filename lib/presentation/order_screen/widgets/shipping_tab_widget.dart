import 'package:flutter/material.dart';

class ShippingTabWidget extends StatelessWidget {
  const ShippingTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("data"),
    );
  }
}
